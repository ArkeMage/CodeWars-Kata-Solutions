std::string no_space(std::string x) {
  
  std::string y;
    
    for (int i = 0; i < x.length(); i++) {
    
      if (x[i] != ' ') { 
        
        y.push_back(x[i]);
        
      }
      
    }

  return y;
        
}
